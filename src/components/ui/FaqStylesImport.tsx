import React from 'react';
import './faq-styles.css';

export const FaqStylesImport: React.FC = () => {
  return null; // This component doesn't render anything, just imports the CSS
};

export default FaqStylesImport;
